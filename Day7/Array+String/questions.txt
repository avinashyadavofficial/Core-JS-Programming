🔷 Array-Based Questions
1. Find the Product of All Elements Except Self
Given [1, 2, 3, 4], return [24, 12, 8, 6], where each element is the product of all other elements except itself.

2. Check If Two Arrays Are Equal (Ignoring Order)
Check if [1, 2, 3] and [3, 2, 1] are the same, ignoring the order.

3. Find the Longest Consecutive Sequence in an Array
For [100, 4, 200, 1, 3, 2], return 4 because [1,2,3,4] is the longest sequence.

4. Find the Missing Number in a Consecutive Array
Given [1, 2, 4, 5, 6], find the missing number (3).

5. Implement a Custom map() Function
Write a function customMap(arr, callback) that behaves like Array.map().

6. Remove Elements That Appear More Than Once
For [1, 2, 3, 2, 4, 5, 1], return [3, 4, 5] (only unique elements remain).

7. Find the Largest Sum of Any Contiguous Subarray (Kadane’s Algorithm)
For [-2, 1, -3, 4, -1, 2, 1, -5, 4], return 6 ([4, -1, 2, 1] has max sum).

8. Find the Median of Two Sorted Arrays
For [1, 3] and [2], return 2.0 (median of [1,2,3]).

9. Find All Triplets That Sum to Zero
For [-1, 0, 1, 2, -1, -4], return [[-1, -1, 2], [-1, 0, 1]].

10. Find the Element That Appears More Than n/2 Times (Majority Element)
For [3, 3, 4, 2, 3, 3, 5, 3], return 3.

🔷 String-Based Questions
11. Check If a String Contains Only Unique Characters
For "abcdef" → return true.
For "aabbcc" → return false.

12. Find the Most Frequent Word in a String
For "apple banana apple orange banana banana", return "banana".

13. Reverse a String Without Using .reverse()
For "hello", return "olleh".

14. Remove All Duplicate Characters from a String
For "aabbcde", return "cde".

15. Convert a Snake_Case String to CamelCase
For "hello_world_test", return "helloWorldTest".

16. Extract All Capital Letters from a String
For "Hello World JavaScript", return "HWJ".

17. Find the First Repeating Character in a String
For "abcdefa", return "a".

18. Find the Longest Common Prefix of an Array of Strings
For ["flower", "flow", "flight"], return "fl".

19. Convert a String into an Acronym
For "Machine Learning Algorithm", return "MLA".

20. Find the Number of Vowels in a String
For "JavaScript", return 3 (a, i, a).

